# =============================================================================
# SECRETS - Copy this to /etc/ckpool/.env.secrets
# =============================================================================
# These credentials are stored outside the project directory
# and are not accessible to Claude Code.
#
# Setup:
#   sudo mkdir -p /etc/ckpool
#   sudo chown brrr:brrr /etc/ckpool
#   sudo chmod 700 /etc/ckpool
#   cp .env.secrets.example /etc/ckpool/.env.secrets
#   chmod 600 /etc/ckpool/.env.secrets
#   nano /etc/ckpool/.env.secrets  # Edit with real values
# =============================================================================

BITCOIN_RPC_USER=your_rpc_username
BITCOIN_RPC_PASS=your_rpc_password
